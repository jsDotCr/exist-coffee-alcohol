export const initialState = {
  error: false,
  isFetching: false,
  type: null,
  quantity: 0
}

// Reducers
export function reducer (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
